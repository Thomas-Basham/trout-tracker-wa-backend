{
  "version": 2,
  "devCommand": "uvicorn api/main:app --host 0.0.0.0 --port 8000",
  "builds": [
    {
      "src": "wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "wsgi.py"
    }
  ]
}
